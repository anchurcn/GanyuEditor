# Agents Workflow

本文档记录本项目中与 AI Agent 协作时，关于**需求文档**与**开发过程 devlog** 的约定。

它不是通用开发规范，也不重复正常的需求沟通、代码实现、验证流程；重点说明：当一个任务可能跨多轮对话、需求会调整、实现过程需要追踪时，文档应该怎么建、怎么改、怎么记录。

---

## 1. 文档职责划分

每个较完整的功能任务，建议至少包含两个文档：

```text
<方案文档>.md
<方案文档>.devlog.md
```

二者职责不同：

| 文件 | 作用 |
| --- | --- |
| `<方案文档>.md` | 记录当前最终方案，即“现在决定怎么做” |
| `<方案文档>.devlog.md` | 记录开发过程，即“需求如何变化、做过什么、遇到什么问题” |

原则：

- 方案文档面向后续实现和维护，应尽量保持干净。
- devlog 面向过程追踪，可以记录试错、废弃方案、用户反馈和验证结果。
- 如果需求变化，先更新 devlog，再调整方案文档。

---

## 2. 方案文档写法

方案文档用于沉淀“当前有效方案”。它不应该像聊天记录，也不应该保留太多已废弃细节。

推荐结构：

```text
# <功能/方案名称>

## 1. 目标
## 2. 当前范围
## 3. 不做的内容
## 4. 设计方案
## 5. 接入点/流程
## 6. 实施步骤
## 7. 风险与待确认问题
```

### 2.1 目标

写清楚最终要达成什么效果。

例如：

```text
导入 .mdl 后，在现有骨骼显示基础上，同步显示模型 Mesh。
```

### 2.2 当前范围

只写当前确认要做的内容。

例如：

```text
- 使用 AssimpNet 导入模型。
- 遍历 scene.Meshes 生成 Unity Mesh。
- 暂时使用无贴图材质。
```

### 2.3 不做的内容

明确排除项，避免后续误实现。

例如：

```text
- 不支持多 Body Part 选择。
- 不处理贴图材质。
- 不做动画预览。
```

### 2.4 设计方案

写当前方案，不写所有历史方案。

如果之前方案被推翻，方案文档中只保留新的结论；旧方案放进 devlog。

### 2.5 接入点/流程

用流程描述任务如何进入现有系统。

例如：

```text
现有导入流程创建骨骼
  ↓
调用 Mesh 预览生成逻辑
  ↓
创建 AssimpMeshPreview 子节点
```

### 2.6 风险与待确认问题

只保留当前仍有效的问题。

已经确认、已经废弃、已经解决的问题，不继续放在方案文档里，应转移到 devlog。

---

## 3. devlog 写法

devlog 用于记录过程，允许保留历史变化。

推荐按时间或阶段追加，不回写成“最终总结”。

推荐结构：

```text
# <方案名称> Devlog

> 对应方案文档：`<方案文档>.md`

---

## <日期或阶段名>

### 用户反馈
### 已调整
### 已完成
### 验证情况
### 已知问题
### 下一步计划
```

不是每一节都必须出现，但每次重要变化至少应记录：

- 用户反馈是什么。
- 方案改了什么。
- 代码做了什么。
- 验证结果是什么。
- 还有什么风险。

---

## 4. 需求变化时的处理方式

当用户推翻或收窄需求时，按这个顺序处理：

1. 在 devlog 追加一节，记录用户新的反馈。
2. 在 devlog 记录哪些旧方案被废弃。
3. 修改方案文档，只保留新的当前方案。
4. 如果已经写了代码，再记录代码需要如何调整。

示例：

```text
## 需求调整

### 用户反馈

- 不需要支持多 Body Part。
- 只需要导入模型时同时显示 Mesh。

### 已调整

- 从方案文档中删除 Body Part 显示/隐藏设计。
- 当前方案改为直接渲染 Assimp 导入的全部 Mesh。
```

注意：

- 不要在方案文档中堆叠“旧方案 A / 新方案 B / 最终方案 C”。
- 方案文档只写最终有效方案。
- 变化过程写进 devlog。

---

## 5. 实现过程中的 devlog 记录

每次完成一个阶段后，应追加 devlog，而不是等任务结束再统一补。

推荐记录格式：

```text
## 实现记录

### 已完成

- 新增 xxx.cs。
- 修改 xxx.cs，将功能接入现有流程。
- 更新 package / plugin / 配置文件。

### 验证情况

- IDE diagnostics 未发现问题。
- Unity Console 仍需用户打开项目验证。

### 注意事项

- 如果某依赖加载失败，不影响主流程。
- 下一步需要检查某个坐标转换或资源路径。
```

这样后续接手时可以知道：

- 为什么这样改。
- 哪些地方已经试过。
- 哪些问题不是遗漏，而是暂缓。

---

## 6. 第三方依赖或环境变化的记录

如果任务涉及包、DLL、插件、外部路径，应在 devlog 中单独记录：

```text
## 依赖切换

### 用户反馈

- 原依赖存在 bug。
- 用户提供了本地构建版本。

### 已完成

- 移除 UPM 依赖。
- 复制本地插件到 Assets/Plugins。
- 清理 lock 文件或 registry 设置。

### 说明

- 代码 API 未变，因此业务代码无需大改。
- 后续需要验证 native dll 是否能加载。
```

方案文档中只保留当前依赖方式，例如：

```text
当前使用本地 AssimpNet UnityPlugin，路径为 Assets/Plugins/AssimpNet。
```

---

## 7. 编译错误和修复记录

当用户反馈编译错误时，应在 devlog 中记录：

```text
## 编译修复

### 问题

粘贴用户提供的错误信息。

### 修复

说明改了哪个文件、改了什么。

### 验证

说明是否已通过 diagnostics 或等待 Unity 验证。
```

如果错误来自第三方代码，也要记录，因为这会影响后续升级或重新复制插件时的处理。

---

## 8. 文档更新规则

1. **方案文档保持当前态**
   - 只保留当前有效设计。
   - 删除或改写已废弃设计。

2. **devlog 保留历史态**
   - 用户反馈不删除。
   - 废弃方案不删除，但标明已废弃。
   - 编译错误、依赖切换、验证失败都应记录。

3. **实现前后都可以更新文档**
   - 实现前：写方案和计划。
   - 实现中：写调整和问题。
   - 实现后：写完成项和验证结果。

4. **最终回复引用文件路径**
   - 告诉用户改了哪个方案文档。
   - 告诉用户 devlog 记录了哪次变化。

---

## 9. 推荐文件命名

方案文档建议放在：

```text
docs/<topic>-plan.md
```

对应 devlog：

```text
docs/<topic>-plan.devlog.md
```

如果是项目级工作流或约定，可以放在根目录：

```text
AgentsWorkflow.md
```

---

## 10. 一句话总结

方案文档记录“当前怎么做”，devlog 记录“为什么变成这样”。

